import type { BgArtistMetadata } from './bg-artist-metadata'

type BgImageMetadata = {
    url: ImageMetadata
    artistMetadata: BgArtistMetadata
    pageBg: string
}
export type { BgImageMetadata }
